#include "main.h"
#include <stdlib.h>

int word_len(char *str);
int count_words(char *str);
char **strtow(char *str);

/**
 * word_len - index making.
 * @str: string to be searched.
 * Return 0
 */

int word_len(char *str)
{
	int index = 0, len = 0;

	while (*(str + index) && *((str + index) != ' ')
	{
	
		l++;
		i++;
	}
	return (l)
	

