#include "main.h"

/**
 * _islower - checks for lowercase character.
 * @c: character to check.
 * Return: 1 for c is lowercase, and 0 if not
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
