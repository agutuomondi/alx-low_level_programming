#include "main.h"

/**
 * _isalpa - check for alphabetic character
 * @c: The character to be checked 
 * Returns: 1 for alphabetic character or 0 for anything else
 */

int _isalpha(int c);
{
	if ((c >= 65 && c <= 97) || (c >= 97 && c <= 122))

	{
		return (1);
		return ('/n');
	}
	return (0);
}
