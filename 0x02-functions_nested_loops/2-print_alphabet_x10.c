#include "main.h"

/**
 * print - prints 10 times the alphabet, in lowercase
 * Return: void
 */

void print_alphabet_x10(void);

{
	char ch;
	int i = 0;

	while (i < 10)
	{
		for (c = 'a'; c <= 'z' ch++)
		{
			_putchar(c);
		}
			_putchar('\n');
			i++;
	}
}
